{
  "name": "fluent-vue-monorepo",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "ls-lint": "ls-lint",
    "prepare": "husky",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^9.3.0",
    "@changesets/cli": "^3.0.2",
    "@changesets/get-github-info": "^1.0.1",
    "@ls-lint/ls-lint": "^2.3.1",
    "eslint": "^10.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0"
  },
  "lint-staged": {
    "*.js": "eslint --fix",
    "*.ts?(x)": "eslint --fix",
    "*.md": "eslint --fix",
    "*.json": "eslint --fix",
    "*.yml": "eslint --fix"
  }
}
